
@{

    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.Title = "Contact US";
}
<h4>
    @(Html.Label("Contact"))    
</h4>
<hr>
@using (Html.BeginForm("Login", "Login", FormMethod.Post))
{
    @Html.AntiForgeryToken()
    <div style="margin-left:auto;margin-right:auto;">
        @Html.ValidationSummary(true)
        <ul>
            @*<li>
                <label for="fullname" class="required">Name</label>
                @(Html.Kendo().TextBox()
                    .Name("fullname")
                    .HtmlAttributes(new { placeholder = "Enter full name", required = "required", validationmessage = "Enter {0}", style = "width:300px" })
                )
            </li>
            &nbsp;
            <li>
                <label for="email" class="required">Email</label>
                @(Html.Kendo().TextBox()
                    .Name("email")
                    .HtmlAttributes(new { placeholder = "Your email address", required = "required", validationmessage = "Enter {0}", style = "width:300px" })
                )
            </li>
            &nbsp;
            <li>
                <label for="messsage" class="required">Your Full Message</label>
                @(Html.Kendo().Editor()
                    .Name("messsage")
                    .HtmlAttributes(new { placeholder = "Your query", required = "required", validationmessage = "Enter {0}", style = "width:300px; height: 350px;" })
                )
            </li>*@
            &nbsp;
            <li class="confirm">
                <button class="k-button k-primary" type="submit">Submit</button>
            </li>
        </ul>

    </div>
}